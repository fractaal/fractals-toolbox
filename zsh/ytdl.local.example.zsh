# Machine-specific path aliases for ytdl.
# Copy to ytdl.local.zsh and customise.  That file is gitignored.
typeset -gA FRACTAL_YTDL_PATH_ALIASES
FRACTAL_YTDL_PATH_ALIASES=(
  stock-music   "$HOME/Stock/Music"
  samples       "$HOME/Music/Samples"
)
